[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "character-kernel"
version = "0.1.0"
description = "Deterministic core contracts for procedural modular character authoring."
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "46slv"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
